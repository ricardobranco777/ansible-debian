---
- name: Install Samba packages
  apt:
    name:
      - samba
    state: present

- name: Create smb.conf
  copy:
    src: smb.conf
    dest: /etc/samba/smb.conf
    owner: root
    group: root
    mode: 0600
